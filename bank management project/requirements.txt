Python Requirements :

1. First, We need to install mysql.connector() module for making connection between Python and MySQL
		
Run the command in cmd/terminal : '''pip install mysql.connector'''


MySQL Requirements :

Tables And Database Requirement For Code To Run Properly

1. Database Creation :-

CREATE DATABASE bank_management_cse;

2. Use This Database Only For New Table Creation :-

USE bank_management_cse;

3. Create Table To Store Login And Account Details :-

CREATE TABLE accounts(
    account_no INT PRIMARY KEY,
    name VARCHAR(100),
    dob DATE,
    phone VARCHAR(15),
    email VARCHAR(100),
    account_type VARCHAR(20),
    pin VARCHAR(10),
    balance FLOAT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

4. Create Table To Store KYC Information Of User :-

CREATE TABLE kyc(
    account_no INT PRIMARY KEY,
    aadhar VARCHAR(12),
    pan VARCHAR(10),
    address TEXT,
    status VARCHAR(20) DEFAULT 'Unverified',
    FOREIGN KEY (account_no) REFERENCES accounts(account_no)
);

5. Create Table To Store Deposit And Withdraw Transaction History :-

CREATE TABLE transactions(
    id INT AUTO_INCREMENT PRIMARY KEY,
    account_no INT,
    type VARCHAR(15),      -- deposit / withdraw
    amount FLOAT,
    date DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (account_no) REFERENCES accounts(account_no)
    flag VARCHAR(20) DEFAULT 'OK'
);

6.  Create Table To Store Wrongful Transaction Reports :-
CREATE TABLE wrongful_reports(
    id INT AUTO_INCREMENT PRIMARY KEY,
    transaction_id INT,
    account_no INT,
    reason TEXT,
    report_date DATETIME DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(20) DEFAULT 'Pending',
    FOREIGN KEY (transaction_id) REFERENCES transactions(id),
    FOREIGN KEY (account_no) REFERENCES accounts(account_no)
);



7. Run The Python Code 'main.py' After Creating All Tables And Make Sure MySQL Is Connected Properly.


---

NOTE :-
Don't change any table name or column name unless you want an error.
KYC status must be only 'Verified' or 'Unverified'.
For transaction history to show properly, do at least 2–3 deposits/withdraws.
If random account number is generated, make sure it is unique (already handled in code).

